
import { environment } from './environments/environment';
var host = environment.api;
export const Config =
{
    HostUrl: host,
    ApiUrl: host + "api/",
    StorageUrl: host + "storage/",
    MobileNumber: "046123653"
}
